version: "3"
services:
  web.service:
    build:
      context: .
      dockerfile: Dockerfile.local
    container_name: web-container
    ports:
      - 80:80
    volumes:
      - ./public:/var/www/html
    tty: true
